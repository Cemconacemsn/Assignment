# Goal

Set up assignment deliverables for documenting AI collaboration: a `/prompts/` iteration log and an `AI_USAGE.md` reflection file.

# Context provided

- Required structure per iteration: Goal, Context provided, Prompt, Output evaluation, Iteration notes.
- Repository state at the time: Maven `insider-assignment` skeleton (Java 17, JUnit 5), default `App` / `AppTest`, no Selenium or careers-site tests.
- Git history showed only `init maven project structure` and `Initial commit`.

# Prompt

> Document your collaboration as follows:
> `/prompts/` directory — one file per prompt iteration. Each file should contain:
> - Goal — what you asked the AI to produce
> - Context provided — what code, page structure, or constraints you fed in
> - Prompt — the actual prompt you sent
> - Output evaluation — what you accepted, what you rejected, why
> - Iteration notes — what changed in the next prompt
> `AI_USAGE.md` — short written answers (2-3 sentences each, in your own words):

*(The message ended before the reflection questions were listed.)*

# Output evaluation

The AI proposed creating `prompts/01-document-ai-collaboration.md` and `AI_USAGE.md` with draft reflection answers and placeholders for missing brief sections. That approach was accepted in principle because it matches the required format without inventing test automation work that had not happened yet. Fabricated Selenium scenarios or multiple fake prompt iterations were rejected to keep the log accurate.

# Iteration notes

In the next prompt, the focus shifted to creating the `/prompts` folder with one markdown file per interaction and the five-section format only (without necessarily adding `AI_USAGE.md` in the same step).
